# macOS
.DS_Store

# Editor / OS clutter
*.swp
*~
.idea/
.vscode/
Thumbs.db

# Environment & secrets — never commit these
.env
.env.*
*.local

# Node (if ever added)
node_modules/

# Agent/skill local runtime files
.claude/
.opencode/
.cline/

# Capture pipeline caches (regenerated by scripts/capture-screenshots.mjs)
scripts/.capture-cache/
scripts/.capture-prepared/
# Local-only docs (not public)
docs/WHY.md
